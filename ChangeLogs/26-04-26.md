# 2026-04-26 架构文档更新记录

## 背景与目标

本次更新目标是把当前项目的真实架构和运行逻辑沉淀到 `AGENTS.md`，方便后续 agent 和维护者快速理解 Web-only 架构、job pipeline、前端工作台、运行时配置和 Memory V3 模块。

更新前 `AGENTS.md` 末尾存在一个较旧且偏 Memory 局部的 `## 12. 项目结构` Mermaid 图，无法完整覆盖当前系统的后端接入层、JobManager 生命周期、三阶段论文管道、前端状态流和独立 Memory 模块逻辑。

## 影响范围

- 文档：`AGENTS.md`
- 变更记录：`ChangeLogs/26-04-26.md`
- 代码只读梳理范围：`server/`、`modules/`、`utils/`、`web/src/`、既有 README / CLAUDE / AGENTS 文档

## 核心改动

- 将旧的 `## 12. 项目结构` 整块替换为 `## 12. 项目架构与运行逻辑`。
- 新增总体架构说明，覆盖 Web 工作台、FastAPI 接入层、后端 routers、JobManager、论文处理模块、工具与数据层。
- 新增总体架构 Mermaid 图，串联前端页面、API、核心模块和本地/远端存储。
- 新增 Job 创建与运行生命周期说明及 Mermaid 图，覆盖 auto/manual 创建、ContextVar、WebSocket、artifact readiness、R2 cleanup。
- 新增三阶段论文管道说明及 Mermaid 图，覆盖 selector、manual source、processor、interpreter、report variants。
- 新增前端工作台运行逻辑说明及 Mermaid 图，覆盖 Run、Reports、ReportViewer、Profiles、MemoryWorkspace、Settings 与共享 client/hook/lib。
- 新增运行控制与清理语义、运行时配置与模型调用说明。
- 新增独立 `## 13. Memory 模块架构与运行逻辑`，用文字和 Mermaid 单独描述 Memory V3。

## 数据结构 / API / 页面变化

- 无代码、数据库、API 或页面行为变化。
- 本次只更新 agent-facing 文档和 changelog。

## 验证结果

- 通过多个 subagent 对后端、三阶段管道、Memory V3、前端工作台进行只读代码梳理后整合文档。
- 已人工检查 Markdown 层级、代码块闭合和 Mermaid 图结构的可读性。
- 未运行后端测试或前端 build，因为本次未修改运行时代码。

## 兼容性与迁移约束

- 不涉及迁移。
- 文档延续现有约定：Run 页 override 不修改 `config.yaml`，job 产物按 `job_id` 隔离，Memory 以 profile 为隔离边界，delete/force-stop/purge 保持纯本地清理。

## 清理情况

- 未新增临时脚本或临时数据文件。
- 仅新增正式 changelog 文件。
